#include <bits/stdc++.h>
int main() {
	std::ios::sync_with_stdio(false);
	std::cin.tie(nullptr);
	int n, k = 0;
	std::cin >> n;
	for (int i = 0; i < n; ++i) {
		for (int j = 0; j < k; ++j) {
			
		}
	}
	return 0;
}