#include <bits/stdc++.h>
#include "testlib.h"
int main() {
	std::ios::sync_with_stdio(false);
	std::cin.tie(nullptr);
	std::string s;
	std::cin >> s;
	// int n = s.size();
	// for ()
	return 0;
}